import { LocaleBasedDictionary } from '../../types'
import en from './en'
import de from './de'

const localeBasedDictionary: LocaleBasedDictionary = {
    en,
    de,
}

export default localeBasedDictionary
